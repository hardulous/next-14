import { Metadata } from "next";

export const metadata: Metadata = {
  title: {
    absolute:"Blog First Ignore"
  }
};

const BlogFirst = () => {
  return (
    <div>BlogFirst</div>
  )
}

export default BlogFirst

// This page component will map to "localhost:3000/blog/first"