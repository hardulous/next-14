import React from 'react'

const InterceptingF3 = () => {
  return (
    <div>(..)f3 InterceptingF3</div>
  )
}

export default InterceptingF3

// Here the meaning of (..)f3 is we are saying when navigating from "/f4" to "/f3" via UI use the route present in (..)f3 folder instead of f3 present in one level above with f4 folder. 